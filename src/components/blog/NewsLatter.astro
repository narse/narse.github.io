---
import type { CollectionEntry } from "astro:content";

interface Props {
  relatedPosts: CollectionEntry<"posts">[];
}

const { relatedPosts } = Astro.props;

const formatDate = (date: Date | undefined) => {
  if (!date) return "";
  return new Intl.DateTimeFormat("ko-KR", {
    year: "numeric",
    month: "short",
    day: "numeric",
  }).format(date);
};
---

{relatedPosts.length > 0 && (
  <div class="mx-auto max-w-6xl pb-10 md:pb-16">
    <div class="rounded-xl bg-gradient-to-br from-blue-50 to-cyan-50 p-6 sm:p-8">
      <h3 class="mb-6 text-xl font-bold text-gray-800">📚 관련 글 더 보기</h3>
      <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {relatedPosts.map((post) => (
          <a 
            href={`/post/${post.id}`} 
            class="group block rounded-lg bg-white p-4 shadow-sm transition hover:shadow-md hover:-translate-y-1"
          >
            <span class="inline-block rounded-full bg-blue-100 px-2 py-0.5 text-xs text-blue-600 mb-2">
              {post.data.category}
            </span>
            <h4 class="font-medium text-gray-800 group-hover:text-blue-600 line-clamp-2">
              {post.data.title}
            </h4>
            <p class="mt-2 text-xs text-gray-500">
              {formatDate(post.data.publishedAt)}
            </p>
          </a>
        ))}
      </div>
    </div>
  </div>
)}
