---
import { FOOTER_COPYRIGHT_NAME, FOOTER_NAV_LINKS } from "@/constants";
---

<footer class="bg-gray-50 border-t border-gray-200">
  <div class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">
    <div class="flex flex-col items-center gap-4 sm:flex-row sm:justify-between">
      <a href="/" class="flex items-center gap-2 text-gray-600 hover:text-gray-900">
        <span class="text-xl">🏛️</span>
        <span class="font-semibold">{FOOTER_COPYRIGHT_NAME}</span>
      </a>
      
      <nav class="flex gap-4">
        {
          FOOTER_NAV_LINKS.map((link) => (
            <a
              href={link.href}
              class="text-sm text-gray-500 hover:text-blue-600 transition"
            >
              {link.label}
            </a>
          ))
        }
      </nav>
    </div>
    
    <div class="mt-6 pt-6 border-t border-gray-200">
      <p class="text-center text-sm text-gray-500">
        &copy; {new Date().getFullYear()} {FOOTER_COPYRIGHT_NAME}. 모든 정보는 참고용이며 정확한 내용은 공식 채널을 확인해주세요.
      </p>
    </div>
  </div>
</footer>
